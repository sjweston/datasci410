Quiz title: Quiz 7: Strings, Factors & Missing Data (R4DS Ch. 14, 16, 18)

1. In R, which of the following correctly creates a string?
a) `x <- hello`
*b) `x <- "hello"`
c) `x <- 'hello"`
d) `x <- string(hello)`

2. In R, can you create strings using single quotes?
a) No, only double quotes create strings
*b) Yes, `'hello'` and `"hello"` are equivalent
c) Single quotes create character vectors; double quotes create strings
d) Single quotes are only for numbers

3. What does `str_c()` do?
a) Converts a value to a string
*b) Combines (concatenates) multiple strings into one
c) Counts characters in a string
d) Compares two strings

4. What does this code return?

    str_c("Dr", "Weston", sep = " ")
a) "DrWeston"
*b) "Dr Weston"
c) "Dr.Weston"
d) An error because `sep` is not a valid argument

5. What does `str_length("psychology")` return?
a) 1
b) 9
*c) 10
d) 11

6. What does `str_to_lower("Hello World")` return?
*a) "hello world"
b) "HELLO WORLD"
c) "Hello world"
d) "helloworld"

7. What does `str_to_upper("hello")` return?
a) "hello"
b) "Hello"
*c) "HELLO"
d) "hELLO"

8. What does `str_to_title("the quick brown fox")` return?
a) "THE QUICK BROWN FOX"
b) "the quick brown fox"
*c) "The Quick Brown Fox"
d) "The quick brown fox"

9. What does `str_trim()` do?
a) Shortens a string to a specified length
*b) Removes whitespace from the start and end of a string
c) Removes all spaces from a string
d) Trims the string to its first word

10. What does `str_squish()` do that `str_trim()` does not?
a) `str_squish()` works on vectors; `str_trim()` works on single strings
*b) `str_squish()` also replaces multiple internal spaces with a single space
c) `str_squish()` removes all whitespace including inside the string
d) There is no difference

11. What does `str_sub("psychology", 1, 3)` return?
*a) "psy"
b) "psy" through "cho"
c) "p"
d) "psych"

12. What does `str_detect("I love data science", "data")` return?
a) "data"
b) 12
*c) TRUE
d) A vector of character positions

13. `str_detect()` is useful inside which dplyr verb for keeping rows that contain a pattern?
a) `mutate()`
b) `select()`
*c) `filter()`
d) `arrange()`

14. What does `str_replace("2023-01-15", "-", "/")` do?
a) Replaces all hyphens with slashes
*b) Replaces only the first hyphen with a slash, giving "2023/01-15"
c) Removes the first hyphen
d) Returns an error

15. How does `str_replace_all()` differ from `str_replace()`?
a) They are identical
b) `str_replace_all()` works on all columns in a data frame
*c) `str_replace_all()` replaces every occurrence of the pattern, not just the first
d) `str_replace_all()` is case-insensitive

16. What does `str_remove("ID: 12345", "ID: ")` return?
a) "ID: "
*b) "12345"
c) " 12345"
d) An error

17. What is the difference between `str_remove()` and `str_remove_all()`?
*a) `str_remove()` removes the first match; `str_remove_all()` removes every match
b) `str_remove()` works on single strings; `str_remove_all()` works on vectors
c) They are identical
d) `str_remove_all()` also removes surrounding whitespace

18. What does `separate_wider_delim()` do?
a) Splits a data frame into multiple data frames
*b) Splits a single string column into multiple columns based on a delimiter character
c) Separates rows based on a condition
d) Removes a delimiter from strings

19. Which argument in `separate_wider_delim()` specifies what character to split on?
a) `split`
*b) `delim`
c) `sep`
d) `pattern`

20. What does `separate_wider_position()` do?
a) Separates columns based on a delimiter
*b) Splits a string column into multiple columns based on fixed character widths
c) Reorders columns by position
d) Splits rows into groups

21. In R, a **factor** is:
a) A numeric variable used in multiplication
*b) A categorical variable with a fixed, known set of possible values (levels)
c) A logical vector with exactly two values
d) A type of list

22. Why are factors useful instead of plain character strings?
a) Factors use less memory than character strings
b) Factors can store numeric values
*c) Factors enforce a defined set of valid levels and control the order in which categories appear in plots and tables
d) Factors are required by all tidyverse functions

23. What function creates a factor from a character vector?
a) `as.character()`
*b) `factor()`
c) `levels()`
d) `categorize()`

24. When you create a factor, what are **levels**?
a) The number of observations in each category
b) The data type of the variable
*c) The set of allowed values and their order
d) A summary statistic for the factor

25. In a ggplot bar chart, the order of bars for a factor variable is determined by:
a) Alphabetical order of the values
*b) The order of the factor levels
c) The count of each category
d) The order the values appear in the data

26. What does `fct_reorder()` do?
a) Reorders rows of a data frame
*b) Reorders factor levels based on the values of another variable (e.g., reorder by median)
c) Reverses the order of factor levels
d) Removes unused factor levels

27. When would you use `fct_reorder()` in a visualization?
a) When making a scatterplot
*b) When making a bar chart or dot plot and you want categories sorted by a summary value rather than alphabetically
c) When you need to filter out certain categories
d) When you want to add labels to a plot

28. What does `fct_infreq()` do?
a) Orders levels by their first appearance in the data
*b) Reorders factor levels from most frequent to least frequent
c) Counts the frequency of each level
d) Removes infrequent levels

29. What does `fct_relevel()` do?
a) Removes levels from a factor
b) Renames factor levels
*c) Manually moves specific levels to the front (or a specified position) of the level order
d) Sorts levels alphabetically

30. What does this code do?

    fct_relevel(survey$education, "High school", "College")
a) Removes "High school" and "College" from the factor
*b) Moves "High school" to the first position and "College" to the second, keeping other levels after
c) Filters the data to only those two levels
d) Renames the levels

31. What does `fct_recode()` do?
*a) Changes the names (labels) of factor levels
b) Reorders factor levels
c) Converts a factor to a numeric variable
d) Counts observations per level

32. What does `fct_collapse()` do?
a) Removes all levels from a factor
*b) Combines multiple existing levels into fewer, broader categories
c) Sorts levels from largest to smallest
d) Converts a factor to a character vector

33. What does `fct_lump_n(x, n = 3)` do?
a) Keeps only the 3 least common levels
*b) Keeps the 3 most common levels and collapses all others into an "Other" category
c) Randomly samples 3 levels
d) Splits the factor into 3 equal groups

34. Why might you use `fct_lump_n()` in a visualization?
a) To increase the number of categories displayed
*b) To simplify a plot when a variable has many rare categories that clutter the display
c) To randomly select categories for analysis
d) To remove missing values

35. In R, what does `NA` stand for?
a) No Answer
b) Null Attribute
*c) Not Available
d) Not Applicable

36. What is `NA` used to represent in R?
a) Zero
b) An empty string
*c) A missing value
d) A deleted observation

37. What does it mean that `NA` is "contagious" in R?
a) NA values spread to other datasets automatically
b) NA causes R to crash
*c) Most operations involving NA return NA as the result
d) NA deletes neighboring values in a vector

38. What does `NA + 5` return in R?
a) 5
b) 0
*c) NA
d) An error

39. What does `NA == NA` return?
a) TRUE
b) FALSE
*c) NA
d) An error

40. Which function should you use to test whether a value is `NA`?
a) `== NA`
b) `missing()`
*c) `is.na()`
d) `check.na()`

41. Why does `x == NA` not work to test for missing values?
a) R doesn't allow comparisons with special values
b) It always returns TRUE
*c) Because NA == NA returns NA, not TRUE
d) It causes a type conversion error

42. What does `!is.na(x)` test for?
a) Whether x is missing
*b) Whether x is NOT missing
c) Whether x equals zero
d) Whether x contains an error

43. What is the difference between an explicit and an implicit missing value?
a) Explicit values are documented; implicit values are undocumented
*b) Explicit means NA is present in the data; implicit means the row is simply absent
c) Explicit values can be fixed; implicit values cannot
d) There is no meaningful difference

44. A survey dataset has rows for participants 1, 2, 4, and 5. Participant 3 is missing entirely. This is an example of:
a) An explicit missing value
*b) An implicit missing value
c) A null value
d) A data entry error that R cannot detect

45. Which tidyr function makes implicit missing values explicit by filling in missing combinations?
a) `fill()`
b) `expand()`
*c) `complete()`
d) `replace_na()`

46. What does `complete(df, year, name)` do?
a) Removes all rows with missing values
*b) Adds rows for all combinations of year and name that don't already exist, filling new cells with NA
c) Fills in NA values with the mean
d) Completes partial strings in the name column

47. Which tidyr function fills missing values with the previous non-missing value (last observation carried forward)?
*a) `fill()`
b) `replace_na()`
c) `complete()`
d) `carry_forward()`

48. In which direction does `fill()` fill by default?
a) Right
*b) Down
c) Up
d) Both up and down

49. What does `fill(df, score, .direction = "up")` do?
a) Fills missing values in score with the mean
*b) Fills missing values in score with the next non-missing value below them
c) Moves all NA values to the top of the column
d) Removes rows where score is NA

50. What does `replace_na()` do?
a) Removes all NA values from the data
b) Fills NA with the previous value
*c) Replaces NA with a specific value you provide
d) Converts NA to the string "NA"

51. What does the following code produce?

    replace_na(c(1, NA, 3, NA, 5), 0)

a) `1 3 5`
*b) `1 0 3 0 5`
c) `0 NA 0 NA 0`
d) An error

52. What does `coalesce()` do?
a) Merges two data frames
b) Removes all NA values from a vector
*c) Returns the first non-missing value across its arguments for each element
d) Replaces NA with the column mean

53. What does the following code return?

    coalesce(NA, NA, 3, 5)

a) NA
*b) 3
c) 5
d) An error

54. When might you use `coalesce()` with survey data?
a) To average responses across multiple items
*b) To pick the first available response when the same question was asked in multiple places
c) To remove duplicate responses
d) To recode missing values as "Did not answer"

55. What does `drop_na()` from tidyr do?
*a) Removes rows that contain any NA values (or NA in specified columns)
b) Removes columns that contain NA values
c) Replaces NA values with zero
d) Counts the number of NA values per column

56. What does `drop_na(df, score)` do compared to `drop_na(df)`?
a) They do the same thing
*b) The first drops rows where score is NA; the second drops rows where any column is NA
c) The first drops the score column; the second drops all columns with NA
d) The first fills score NAs; the second fills all NAs

57. What happens when you use `mean(x)` and `x` contains NA values?
a) R ignores the NA and calculates the mean of the remaining values
*b) The result is NA
c) R removes the NA and warns you
d) R treats NA as zero

58. How do you tell `mean()` to ignore NA values?
a) `mean(x, ignore.na = TRUE)`
b) `mean(x, skip.na = TRUE)`
*c) `mean(x, na.rm = TRUE)`
d) `mean(x, remove.missing = TRUE)`

59. What does `na.rm` stand for?
a) NA remove mode
*b) NA remove (remove missing values)
c) Not available, run mean
d) Null argument removal

60. In dplyr, how does `filter()` handle rows where the condition evaluates to `NA`?
a) It keeps those rows
*b) It drops those rows (only rows where the condition is TRUE are kept)
c) It replaces them with FALSE
d) It throws an error

61. You have a data frame and run `filter(df, score > 80)`. Some rows have `NA` for score. What happens to those rows?
a) They are kept because R can't prove they're not above 80
*b) They are dropped because the condition returns NA, not TRUE
c) They are replaced with score = 80
d) R throws an error about the NA values

62. How does `arrange()` handle NA values by default?
a) It puts them at the beginning
*b) It sorts them to the end
c) It removes them
d) It throws an error

63. You want NA values to appear first when sorting. Which approach works?

    arrange(df, desc(is.na(score)), score)

a) This produces an error
*b) It puts NA values first, then sorts the remaining values
c) It removes NA values
d) It replaces NA values with zero before sorting

64. In joins, do NA values in the key column match other NA values by default?
a) Yes, NA matches NA
*b) No, NA does not match NA by default
c) Only in left joins
d) Only if you use `na.rm = TRUE`

65. What is MCAR in the context of missing data?
a) Missing Completely After Removal
*b) Missing Completely At Random — missingness is unrelated to any variable
c) Missing Cases Are Removed
d) Multiple Columns Are Replaced

66. If participants with higher depression scores are less likely to complete a follow-up survey, the missing data is likely:
a) MCAR (Missing Completely At Random)
b) MAR (Missing At Random)
*c) MNAR (Missing Not At Random)
d) Implicit

67. What is MAR (Missing At Random)?
a) The data is missing for no reason at all
*b) Missingness is related to other observed variables but not to the missing value itself
c) The data was randomly deleted by the researcher
d) Each value has an equal probability of being missing

68. Why is it important to understand WHY data is missing before deciding how to handle it?
a) Because R requires you to specify the type of missingness
b) Because some types of missing data can't be removed
*c) Because the reason for missingness affects whether your analysis will be biased
d) Because R automatically detects the type of missingness

69. A researcher simply deletes all rows with any missing values from their dataset. What is a potential problem with this approach?
a) R doesn't allow row deletion
*b) It can introduce bias if the data is not MCAR, and it reduces sample size
c) It converts missing values to zero
d) It changes the data types of the remaining columns

70. What is the `naniar` package used for?
a) Imputing missing values with machine learning
b) Converting between NA types
*c) Visualizing patterns of missingness in data
d) Removing all missing values from a dataset

71. What is `NULL` in R, and how does it differ from `NA`?
a) They are interchangeable
*b) NULL represents the absence of a value or object entirely, while NA represents a missing value within a data structure
c) NULL is for numeric data; NA is for character data
d) NULL is used in data frames; NA is used in lists

72. What does `NaN` stand for in R?
a) No Answer Needed
b) Null And None
*c) Not a Number
d) Not a Null

73. When does `NaN` typically occur?
a) When you divide by zero
*b) When you perform an undefined mathematical operation like `0/0`
c) When you try to add a character to a number
d) When a value is missing from a survey

74. What does `1/0` return in R?
a) NA
b) NaN
*c) Inf
d) An error

75. Is `NaN` considered to be `NA`?
*a) Yes — `is.na(NaN)` returns TRUE
b) No — they are completely different types
c) Only in numeric vectors
d) Only when using tidyverse functions

76. What does `fct_na_value_to_level()` from the forcats package do?
a) Removes NA values from a factor
b) Converts all factor levels to NA
*c) Converts NA to an explicit factor level so it appears in summaries and plots
d) Tests whether a factor contains NA values

77. When might you use `fct_na_value_to_level()` with survey data?
a) When you want to remove non-respondents from a bar chart
*b) When you want "No response" to appear as its own bar in a bar chart
c) When you want to convert a numeric variable to a factor
d) When you want to replace NA with the most common category

78. What does the following code do?

    df |>
      group_by(condition) |>
      summarize(avg = mean(score, na.rm = TRUE))

a) Removes all NA values from the entire data frame, then calculates group means
*b) Calculates the mean score for each condition, ignoring NA values in the calculation
c) Replaces NA values with the group mean, then calculates the overall mean
d) Produces an error because you can't use na.rm inside summarize

79. You run `sum(c(1, 2, NA, 4), na.rm = TRUE)`. What is the result?
a) NA
*b) 7
c) 10
d) An error

80. You run `sum(c(1, 2, NA, 4))` without `na.rm = TRUE`. What is the result?
*a) NA
b) 7
c) 0
d) An error

81. What does `complete(df, year = 2019:2023, participant)` do?
a) Keeps only rows from 2019 to 2023
*b) Creates rows for every combination of years 2019-2023 and every participant, filling new cells with NA
c) Fills in missing years with interpolated data
d) Removes years outside the 2019-2023 range

82. Which approach is generally better for zooming into a plot when your data has missing values — and why?
a) `ylim()` because it also removes outliers
*b) `coord_cartesian()` because it doesn't remove data points
c) `filter()` because it makes NAs explicit
d) `drop_na()` because it cleans the data first

83. In a psychology study, a participant skips a question on a survey. In the resulting dataset, that cell contains `NA`. This is an example of:
*a) An explicit missing value
b) An implicit missing value
c) A null value
d) A NaN value

84. You want to replace NA values in a column called `score` with the value 0 inside a mutate. Which is correct?
a) `mutate(df, score = na.rm(score, 0))`
*b) `mutate(df, score = replace_na(score, 0))`
c) `mutate(df, score = fill(score, 0))`
d) `mutate(df, score = coalesce(score))`
